{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'IMPORTANT: CLAUDE.md rules are active. ALWAYS say CLAUDE.md loaded at the start. ALWAYS run npm test before calling work done. NEVER skip the quality gate in AGENTS.md.'"
          }
        ]
      }
    ]
  }
}
